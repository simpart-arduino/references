void setup() {
  // put your setup code here, to run once:
  Serial.begin(38400);
  delay(1000);
}

void loop() {
  // <RD,>??
  // 0x3c, 0x52, 0x4d, 0x2c, 0x3e, 0x3f, 0x3f, 0x0d, 0x0a
  // (0x25) 32 35
  
  // <RM,M01,>??
  // 0x3c 0x52 0x44 0x2c 0x4d 0x30 0x31 0x3e 0x3f 0x3f 0x0d, 0x0a

  // <RD,M01,E51,>??
  // 0x3c, 0x52, 0x44, 0x2c, 0x4d, 0x30, 0x31, 0x2c, 0x45, 0x35, 0x31, 0x2c, 0x3e, 0x3f, 0x3f, 0x0d, 0x0a
  
  char test[] = { 0x3c, 0x52, 0x4d, 0x2c, 0x3e, 0x3f, 0x3f, 0x0d, 0x0a };
  Serial.flush();
  //Serial.print(test);
  Serial.write(test,9);
  
  
  delay(100);

  int rdat = 0;
  int rcnt = Serial.available();
  if (0 < rcnt) {

    for (int i=0;i < rcnt;i++) {
      rdat = Serial.read();
      Serial.print((char) rdat);
    }
  }
  delay(1000);
}
